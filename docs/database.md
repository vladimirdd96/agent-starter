<!-- read_when: database, DB, query, migration, schema, table, column, index, SQL, ORM, Prisma, Drizzle, Supabase -->

# Database

> Replace this file with your project's actual database architecture.

## Provider & ORM

<!-- PostgreSQL via Prisma, Supabase, Drizzle, SQLite via better-sqlite3, etc. -->

## Type generation

<!-- How TypeScript types are derived from schema. e.g.: `npx prisma generate`, `supabase gen types` -->
<!-- IMPORTANT: Never write DB types by hand — always regenerate from source. -->

## Migrations

<!-- How to create and apply migrations:
  Prisma:   npx prisma migrate dev --name <name>
  Supabase: supabase migration new <name> && supabase db push
  Drizzle:  npx drizzle-kit generate && npx drizzle-kit push
-->

## Schema overview

<!-- High-level table list with 1-line descriptions -->

## Query conventions

<!-- Where DB queries live (repositories, services, server actions), naming conventions -->

## Row-level security / access control

<!-- If using Supabase RLS or similar — describe policies and what they guard -->

## Gotchas

<!-- Non-obvious DB behavior: enum caveats, index gotchas, migration ordering issues -->
